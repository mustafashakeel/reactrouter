var React = require('react'),
    Weather = React.createClass({
        render:function(){
          return(
            <h1>Weather Component</h1>
          );
        }
    });

    module.exports = Weather;
