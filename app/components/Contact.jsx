var React = require('react'),
    Contact = React.createClass({
        render:function(){
          return(
            <h1>Contact Component</h1>
          );
        }
    });

    module.exports = Contact;
